pipeline {
    agent any

    stages {
        stage('Test Git Checkout') {
            steps {
                echo 'GitHub to Jenkins connection working'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
